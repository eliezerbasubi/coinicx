import { MarketType } from "@/types/market";

export const supportedPaths: MarketType[] = ["buy", "sell"];
export const defaultMarketType: MarketType = "buy";

export const defaultAssetsCode = { fiat: "USD", crypto: "BTC" };
